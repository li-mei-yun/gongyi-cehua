# 将曲线改成“角度横轴、扭矩纵轴”

## 修正后的含义

- 横轴：原始角度，单位度。
- 纵轴：原始扭矩，单位 N·m。
- 按采样顺序连接点。角度回退、重复角度、负值均保留；不排序、不平滑、不重新判定质量。
- 保留原始 series 数值及两个整条曲线标签，不把数据只剩成图片。
- 采用内嵌 SVG 图显示，可通过 Image 控件缩放；不提供逐点悬停读数，也不把整条曲线标签变成区间标签。

你的示意图编号是 5898714；生成的预览编号是实际提供的 3918223。二者坐标范围不同是正常的，本次没有将角度归一化、清零、截断或强行限制到 -200～200 度。

## 当前状态

- 原导入记录显示：项目 **3** 已成功导入 **339 条**，没有未确认批次。
- 新的独立包 `package-scu2020-xy` 包含全部 **12,981 条、7,831,497 个采样点**。
- 原始数据与分类标注逐条核对一致。新增矢量图后，包约 **1.06 GB（十进制）**，重新分成 **154 个不超过 8 MiB 的批次**。
- 原 `package-scu2020` 和导入记录没有改动。
- 新模板已通过当前运行的 Label Studio 校验器；已检查 PNG 图像的坐标及曲线，矢量图使用同一个绘图函数。
- 尚未在用户项目内运行修正，也未验证实际账户页面的最终显示。浏览器安全策略阻止了本地 SVG 页面预览；没有绕过这一限制。

## 第一步：修正现有的339条

先不要运行旧包的继续导入命令，也不要在修正期间手动编辑任务或导入数据。

PowerShell 运行下面一行（使用已安装的内置 Python；修正阶段只读取已生成图，不需要安装绘图库）：

```powershell
& 'C:\Users\MGA\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe' 'C:\Users\MGA\Documents\ChatGPT\工艺策划\label-studio-setup\batch-curves\upgrade_xy.py' --project 3 --apply
```

出现确认提示时输入：

```text
UPGRADE 3
```

然后在隐藏输入提示里粘贴自己的 Label Studio 访问令牌，回车。不要发到聊天里。

工具会：

1. 验证项目、现有任务数量、曲线编号、原始采样数据和导入记录。
2. 将当前项目配置及任务数据备份到新包下的 `upgrade-backup-project-3`。
3. 只为现有任务的 data 增加 `angle_torque_plot` 字段。**不 POST 新任务、不删除任务、不 PATCH annotations**；你导入后手工改过的标签也不会用旧 Excel 覆盖。
4. 全部图补齐后，再切换项目的显示模板。原 TimeSeries 对象及分类绑定保留并隐藏，以兼容已有标签。
5. 再读回任务，核对标签未变；为新包生成对应续跑记录，让后续导入跳过这339条。

看到 `UPGRADE DONE` 后刷新项目页面，先打开 3918223 检查坐标、图形与两个标签。

## 第二步：确认显示后再继续其它曲线

只有修正成功并确认网页显示正确后，才运行下列命令。新命令明确指定 **XY 新包**：

```powershell
& 'C:\Users\MGA\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe' 'C:\Users\MGA\Documents\ChatGPT\工艺策划\label-studio-setup\batch-curves\import_batches.py' --package 'C:\Users\MGA\Documents\ChatGPT\工艺策划\label-studio-setup\batch-curves\package-scu2020-xy' --project 3 --apply
```

确认文字仍为 `IMPORT 3`。使用新包中的续跑记录，不会重复导入原来的339条。每批间隔30秒，全部新包的间隔时间约77分钟，实际还要加服务处理时间；可以在批次确认后停止，按相同命令继续。

**不要只把新 XML 手动粘贴进旧项目。** 旧任务缺少新图字段，必须先补齐。也不要用旧包继续导入，它仍然是之前的双通道显示数据。

## 中断与恢复

- 修正过程中断：已完成的任务 PATCH 是可重复检查的，只补齐缺失图；重跑同一个修正命令即可。出现数据或标签不一致则停止并保留备份。
- 若新包的导入日志已经存在，修正工具会停止，避免覆盖后续导入进度。不要为了重跑而删除日志。
- 若任务数量与旧日志不符、项目模板被另行修改、旧导入有未确认批次，工具会拒绝修改，需要先核对。
- 备份可用于恢复旧模板和任务 data；不要用备份 annotations 覆盖后来人工更新的标签。回退时需要核对当前项目状态，不建议盲目恢复整包。

## 本次验证

- 全量原数据/标注保真及新包SHA-256、文件数量验证通过。
- 9项离线自动测试通过，包括非单调/重复角度顺序保留、已人工修改标签保留、PATCH响应丢失后安全续跑、不重复创建任务、旧导入的去重/错误路径。
- 真实 API 修正尚需用户提供本机访问令牌并执行，不能把离线测试当作已修正成功。

参考：[TimeSeries要求有序横轴](https://labelstud.io/tags/timeseries)、[任务更新API](https://api.labelstud.io/api-reference/api-reference/tasks/update)、[Image显示控件](https://labelstud.io/tags/image)。
